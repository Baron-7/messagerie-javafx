<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Connexion à MySQL -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <!-- IMPORTANT : remplace "messagerie_db" par le nom de ta base, et mets ton mot de passe -->
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/messagerie_db?serverTimezone=UTC&amp;useSSL=false</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <!-- Dialecte MySQL 8 -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Hibernate crée ou met à jour les tables automatiquement -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Affiche les requêtes SQL dans la console (utile pour voir ce qui se passe) -->
        <property name="hibernate.show_sql">true</property>

    </session-factory>
</hibernate-configuration>
